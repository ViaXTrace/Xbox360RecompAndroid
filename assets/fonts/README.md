# Xbox Font

Place `XboxOne-Regular.ttf` in this directory.

The Xbox One font can be downloaded from:
https://learn.microsoft.com/en-us/typography/font-list/xbox-one

Or use any free alternative. The font is referenced in `pubspec.yaml` under `family: XboxFont`.
