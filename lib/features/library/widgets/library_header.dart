// This file is intentionally left as a thin shim.
// The header is now inline in LibraryScreen._buildTopBar and _buildSearchAndFilter.
// Keeping the file to avoid breaking existing imports if any.
library;
